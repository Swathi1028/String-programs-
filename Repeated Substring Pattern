class Solution {
    public boolean repeatedSubstringPattern(String s) {
        String str = s + s;
        int n = str.length();
        String sub = str.substring(1, n - 1);
        return sub.contains(s);
    }
}
